# Testing

- Skip tests unless explicitly requested.
- Vitest is configured for unit tests in `apps/web-app`.
- Available commands (only when requested): `pnpm --filter @preza/web-app test`, `pnpm --filter @preza/web-app test:run`.
